server:
  port: 8888
spring:
  datasource:
    name: dataSource
    url: jdbc:mysql://127.0.0.1:3307/snacks?useUnicode=true&characterEncoding=utf8&allowMultiQueries=true
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
  http:
    encoding:
      enabled: true
      charset: utf-8
      force: true

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl